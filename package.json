{
  "name": "tiaju",
  "version": "1.0.0",
  "private": true,
  "description": "Site da campanha Tia Ju 10 123 — HTML estático",
  "scripts": {
    "start": "live-server src",
    "mobile": "live-server src --host=0.0.0.0 --port=5599 --no-browser",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
